name: Messe Eventkalender Worker

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Status ausgeben
        run: echo "GitHub Actions läuft – bereit für Playwright 🚀"
